type: update
name: import

nodes:
  - cloudlets: 22
    nodeGroup: cp
    image: atlassian/jira-software:8.11.1

onInstall:
  - cmd[cp]: free -m | sed -n '2p' | awk '{printf "%.f", $2 * 0.7}'
  - log: ${response.out}
